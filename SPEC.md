# SPEC — あそんでフォント (AsoGlyph)

## 1. コンセプト

幼児が文字を書く練習をすると、その筆跡がそのままフォントになるアプリ。

子供から見れば「文字あそびのアプリ」、親から見れば「子供の字をフォントとして残すアプリ」。
この二面性が製品の中核であり、UI・データ設計はすべてこの分離に従う。

**重要な設計思想**: 子供の字の魅力は、その歪みにある。整った字を書かせることが目的ではない。
採点機能は出題の重み付けにのみ使い、フォントへの採用可否には使わない（例外は鏡文字と明らかな書き損じのみ）。
**その判定はきつくしない。** 取りこぼした鏡文字が1字混じるほうが、その子の字が1字消えるより良い。
判定は子供に告げない。書けなかったことを画面で言われる筋合いはない。

## 2. 対象ユーザー

| | 想定 | 扱い |
|---|---|---|
| 主対象（書く人） | 4歳後半〜6歳（年中〜年長） | 自立して書ける。フォント素材として成立する |
| 副対象（書く人） | 3〜4歳 | なぞり書きモードで入口のみ提供。素材にするかは出力時に選ぶ |
| 管理者 | 保護者 | 管理画面でフォント生成・設定 |

3〜4歳を入口に含めるため、**子供向け画面は文字に依存しないUI（アイコン＋音声＋イラスト）を必須**とする。
後から音声を足すのは高くつくため、L1 の時点で音声読み上げの土台を入れる。

## 3. 技術スタック

- **Flutter**（Web / Android / iOS）。macOS・Windows は優先度低。
- **Web 版は PWA とする**（10.1）。ホーム画面に置けて、オフラインでも開ける。
- **フォント生成は端末内で完結**。サーバーへ筆跡を送信しない（プライバシー・法規制対応の簡素化）。
- ローカルストレージのみ。クラウド同期は当面おこなわない。

## 4. 用語とデータモデル

### 4.1 Sample（試行） — 唯一の実データ

不変・追記のみ。削除しない。
（例外は動作確認用の全消しのみ。管理画面の奥に置き、確認を挟む。）

```dart
Sample {
  id          : String
  userId      : String
  char        : String          // 対象文字 1字
  strokes     : List<Stroke>    // 正規化 em 空間 (0..1000)
  writtenAt   : DateTime
  mode        : trace | copy | free   // なぞり書き / お手本を見て / 何も見ずに
  score       : Score?          // 出題の重み付け用。採用可否には使わない
  rejected    : bool            // 鏡文字・なぐりがき検出時のみ true
}

Stroke = List<Point>
Point  { x: int16, y: int16, t: uint16, pressure: uint8 }   // 約7バイト
```

単語トライアルへの逆参照は持たない。並びは WordAttempt 側（4.2）が持っており、
両方に持たせると、どちらが正かが決まらない場所ができる。

**座標は必ず正規化 em 空間（0–1000）で保存する。** デバイスピクセルで保存すると
スマホとタブレットで書いた字の大きさが揃わず、フォントが破綻する。

容量試算: ひらがな1文字 ≒ 3画 × 40点 ≒ 0.9 KB。46字 × 200回 ≒ 8 MB。
全履歴を保持しても問題にならない。

### 4.2 WordAttempt（単語トライアル）

```dart
WordAttempt {
  id, userId
  word       : String           // 書いた語そのもの
  sampleIds  : List<String>     // 書いた順
  finishedAt : DateTime
}
```

「単語単位で履歴を持つ」という要求の実体。フォント生成の時点指定はこの `finishedAt` を使う。

**最後まで書けたときだけ残す。** 途中でやめたぶんは残さない。書いた字そのものは
1 字ずつ Sample に入っているので、失われるものはない。

**この履歴は消せる**（管理画面で、語ごとにも、まとめても）。4.1 の「削除しない」は
書いた記録（Sample）の話で、こちらは「この語を書き終えた」という印でしかない。
星が付いたままだと、もう一度書かせたい語を子供が選ばなくなる。消しても集めた字は
1 字も減らない。

**単語帳の id ではなく語そのものを持つ。** 取り込んだ単語帳を親が片づけても、
書いた履歴は残る（4.1 の追記のみという原則）。

### 4.3 FontRecipe（版） — 実体を持たない導出ビュー

スナップショットは実体を持たず、**解決規則の集合**として表現する。数百バイト。

```dart
FontRecipe {
  id, userId, name, createdAt
  charSets   : List<CharSet>                          // 出力対象の文字種
  base       : Policy                                 // 既定 = latest
  groupRules : List<{ scope: CharSet, policy: Policy }>
  charRules  : Map<String, String>                    // char -> sampleId
  fontMeta   : FontMeta
}

Policy = latest | best | at(DateTime)
```

**解決順**: `charRules` > `groupRules` > `base`。

- `latest` … その時点で最新の Sample（既定）
- `best` … 測りがいちばん良い Sample。同じ字を何度も書いたとき、そのうちどれを
  採るかを親が選べるようにするもので、**フォントへの採否を点で決めることではない**。
  測りの無い試行（書き順データを持たない字）は比べようがないので、そこでは最新を採る
- `at(t)` … 時刻 t 以前で最新の Sample

`charRules` は、なぞりを混ぜるかの設定にも左右されない。親がその字を名指しで
選んでいるため。指す先の Sample が失われていたら、下位の規則へ落とす。

`base` の既定を `latest`（期間無制限）とする理由: 文字種ごとに収集開始時期が異なる
（ひらがなは4歳、漢字は6歳）ため、単一時点を基準にすると後発の文字種が空になる。
時点指定はオプトイン。

これにより「あの頃の文字」が表現できる:

| やりたいこと | レシピ |
|---|---|
| 2026年春の、ひらがなだけのフォント | `charSets:[hiragana], base:at(2026-04-30)` |
| ひらがなは去年、漢字は今年 | `groupRules` で文字種ごとに時点指定 |
| 今の字。ただし「あ」だけ初めて書けた日の版 | `charRules:{'あ': 初回sampleId}` |

フォント生成は `(FontRecipe, Sample集合) → バイナリ` の**純関数**。
同じレシピからは常に同じフォントが出るため、生成物を保存する必要がない
（記録するのは `{recipeId, format, builtAt}` のみ）。

### 4.4 User

```dart
User { id, displayName, avatar, birthMonth?, createdAt, passcode?
       collecting : Set<CharSet>   // 集める文字種（5）
       wordBooks  : Set<String>    // この人に出す単語帳（7.4）
}
```

`birthMonth` は難易度統計の補正にのみ使う。任意。

`collecting` と `wordBooks` は、どちらも空なら「全部」を意味する。

### 4.5 履歴の刈り込み（当面は実装しない）

容量試算のとおり不要。将来必要になった場合のルールのみ定めておく:

> `charRules` および名前付き FontRecipe から参照される Sample は pin。
> それ以外は「直近90日 ＋ 各文字の月間ベスト」のみ保持。

参照を GC ルートとする1本のルールで済む。**L1〜L5 では実装しない。**

## 5. 文字セット

管理画面で「収集対象」と「出力対象」を**別々に**設定できる。
収集は広く、出力は絞る、という非対称が「あの頃の文字」の前提。

収集対象は**人ごと**に持つ。外した文字種は子供向け画面から丸ごと消える
（4歳にカタカナ81字まで見せると、目の前の字を探せなくなる）。
外しても集めた字は消えない。**最後の1つは外させない**（全部外すと子供の画面が
空になり、何をする画面か分からなくなる）。
出力対象は版（FontRecipe）が持つ（4.3）。

| CharSet | 字数 | 内容 |
|---|---|---|
| `digits` | 10 | 0–9 |
| `hiragana` | 80 | 清音46 ＋ 濁音20・半濁音5 ＋ 小書き9 |
| `katakana` | 81 | ひらがなと同じ80 ＋ ー（長音符） |
| `latinUpper` | 26 | A–Z |
| `latinLower` | 26 | a–z |
| `punctuation` | 6 | 、。！？「」 |
| `kanjiG1` … `kanjiG6` | 80 / 160 / 200 / 202 / 193 / 191 | 学年別漢字配当表（2020年度〜、計1026字） |

### 5.1 濁音・半濁音の収集方針

**濁音20字・半濁音5字は直接書かせる。** 清音46字と合わせて71字。

清音46字 ＋ 「゛」「゜」を集めて出力時に合成する案は**採らない**。濁音は濁点を足しただけの
字ではなく、その子が書いた1つの字として残す。合成すると、濁点の置き場所という
「その子の字ではない情報」（KanjiVG の濁音字から測った矩形）がフォントに混ざる。

字数は増えるが、収集は1字ずつ進むもので一度に全部書かせるわけではない。

**濁音・半濁音は清音と同じ CharSet に入れる。** 子供にとっては同じ「ひらがな」であり、
束が分かれていると自分がどちらを練習しているのか分からなくなる。並びは清音46字を先に、
濁音・半濁音25字をそのうしろに続ける。カタカナも同じ扱いとする。

### 5.2 ラテン文字の扱い

- ラテン小文字はベースライン・xハイト・ディセンダを持つため、**書き取り枠が4本線ガイドになる**
  （漢字・かなの正方枠とは別UI）。
- 字幅は 和文＝全角1000 / 数字・ラテン＝半角500 の固定。プロポーショナルは実装しない。
- 上記のコストゆえ**優先度は最下位**（L5）。

### 5.3 小書きの字の扱い

**小書き（ぁぃぅぇぉっゃゅょ）は書き取り枠を小さくして書かせる。**
枠の一辺は全角の **0.81倍**、左右中央・下揃えに置く。

書き取り面は em 空間にそのまま対応しており（自動での拡大縮小はしない）、
**書いた大きさがそのままフォントの字形になる**。全角の枠いっぱいに書かせると
「ゃ」が「や」と同じ大きさのグリフになり、フォントに載せたときに見分けが付かない。
枠だけが大きさを決めているので、枠を小さくすれば足りる。

出力時に縮小する案は**採らない**。なぞり書きモードでは KanjiVG のお手本（すでに
0.79〜0.80倍で描かれている）をなぞるため、そこから更に縮めると二重に小さくなる。
また、書いたものと出てくる字形が一致しなくなり、ラスタトレース方式の利点（8.1）を失う。

0.81 という値は実測で決めた。ヒラギノ角ゴ W3 の ぁっゃゅょァ は大きい字の 0.81倍、
KanjiVG の同じ字は 0.79〜0.80倍。独立した2つの出典が一致する。
下端は、どちらの出典でも大きい字とほぼ揃う。

**「ー」（長音符）は小書きではない。** 全角の幅いっぱいに引く字なので、枠は変えない。

小書きが無いと拗音「きゃ」と促音「がっこう」が書けず、日常語の相当数が
そのまま出せなくなる。

## 6. 書き順データ

**KanjiVG を採用**（CC BY-SA 3.0 / Ulrich Apel）。

### 6.1 収録範囲（実データで確認済み）

| 文字種 | 収録 |
|---|---|
| 数字 0–9 | あり |
| ラテン大文字・小文字 | あり |
| ひらがな（濁音・半濁音・小書き含む） | あり |
| カタカナ（小書き・ー・ヴ 含む） | あり |
| 漢字 | あり（**学年情報は含まれない**） |
| 、。！・々゛゜ | あり |
| 「」？ | **なし → 自作** |

学年グルーピングは、学習指導要領の学年別漢字配当表を事実データとして自前で保持する
（KANJIDIC2 を持ち込まず、依存ライセンスを増やさない）。

### 6.2 データ形式

- viewBox `0 0 109 109`。**em 1000 への正規化は 1000/109 倍**。
- `<path id="kvg:XXXXX-sN">` の並び順が画順。`kvg:element` に対象文字。
- 別グループに画数ラベルの座標を保持。
- 書き順アニメーションは path の dash offset で実装する。

### 6.3 ライセンス上の不変条件

- アプリ内にクレジット表記（KanjiVG / Ulrich Apel / CC BY-SA 3.0）とライセンスへのリンクを置く。
- KanjiVG を独自形式へ変換して同梱する場合、**変換済みデータも CC BY-SA 3.0 で提供**する
  （変換スクリプトと出力をリポジトリで公開する）。
- **生成フォントは子供の筆跡由来であり KanjiVG の二次的著作物ではない。SA は及ばない。**
- **書体そのものは、日本では原則として著作物にならない。**
  最判平成12年9月7日（ゴナU書体事件）は、印刷用書体が著作物といえるのは
  「従来の書体に比して顕著な特徴を有するといった独創性」と
  「それ自体が美術鑑賞の対象となり得る美的特性」を備える場合に限るとした。通常の字形は満たさない。
  → **なぞって書いた字をフォントに載せても、KanjiVG の著作権は及ばない**と考えられる。
  CC BY-SA は著作権を土台にしたライセンスであり、及ぶ権利が無ければ条件も働かない。
  （なぞり書きを素材に使えるのはこの理由による。7.1）
- ただし **未収集文字を KanjiVG の字形で埋めてはならない**。
  こちらは字形の保護ではなく、KanjiVG の**データを複製して出力へ埋め込む**行為であり話が別。
  加えて、書いていない字を勝手に埋めるのは製品として誤り。
  → **未収集文字は cmap に載せない**ことを設計上の不変条件とする。
- 国外へ配る場合は前提が変わる（ドイツは書体を意匠法で保護、米国はフォントプログラムを保護）。
  配布範囲を広げるときに確認する。

## 7. 機能要件

### 7.1 練習（子供向け画面）

**練習モードは3段階**。

| モード | 内容 | 素材採用 |
|---|---|---|
| `trace` なぞり書き | KanjiVG の字形をなぞる | 出力時に選ぶ |
| `copy` お手本を見て書く | お手本を横に表示して書く | する |
| `free` 何も見ずに書く | 音声で読み上げて書く | する |

**履歴は「なぞり」と「それ以外」に分けて持ち、フォント生成のたびに混ぜるかを選ぶ**（7.7）。
既定は混ぜない。なぞった字はお手本の形をなぞったもので、その子の字とは言いにくいため。
一覧の充足率と星は「なぞり以外で書けた字」を数え、なぞった字は別の印で見せる。

お手本には KanjiVG の書き順を再生し、引き終わったら画ごとの番号と、書き始める向きの
矢印を出す。番号と矢印は線の外に置く（字形の一部に見えると、なぞる子がその形ごと書く）。

**画ごとに色を変える。** 同じ色で引くと、どこで 1 画が終わってどこから次が始まるのかが
分からない。交わる画（「あ」の 2 画目と 3 画目）では、引き終わった字を見ても切れ目が
読めなくなる。番号と矢印もその画と同じ色にして、番号と線を目で結べるようにする。
なぞり書きの下敷きも同じ色で、薄さだけを変える（上から子供が書くため）。
順番そのものは番号が持っているので、**色が読めなくても書き順は分かる**。

**書けたら、押さずに次の字へ進む**（語を書いているとき）。字ごとに「つぎ」を
押させると、書くより押す回数のほうが多くなる。書けた字がフォントの字形に変わる
ところ（SPEC 8.1）は見せてから進む。1 字だけの練習では止まったままにして、
「もういちど」で書き直せる。

**進む前に、ほめ言葉を言い終わる。** 言い終わる前に画面を替えると、次の画面の
読み上げが追い越して声がぶつ切りになる。読み上げの終わりを知らせない端末が
あるので、待つのは 4 秒までにする。読み上げが無い端末でも、書けた字は
0.9 秒は見えるようにする。

**声は 1 字に 1 つ。** 「『ねこ』の『ね』、かいてね」と字ごとに語を繰り返すと、聞きたい
1 字が毎回うしろに回る。語の名前は**最初の 1 字でだけ**言い（「『ねこ』を かこう」）、
あとは「『ね』を かいてね」だけにする。何の語かは画面に出たままになっている。

- 書き取り枠にはマス目・十字補助線を表示する（字の大きさ・位置が揃わないとフォントが破綻する）。
- 1セッションは 3〜5分（3〜4歳）／10〜15分（5〜6歳）を目安に区切る。
- 進捗・報酬（星、コレクション）を提示する。継続率がそのまま収集量になる。
- 左利き対応（手で隠れない位置にUIを配置）。

### 7.2 入力

- `Listener` の PointerEvent から筆圧・傾きを取得。`PointerDeviceKind.stylus` を判別。
- スタイラス使用中はタッチ入力を無視（パームリジェクション）。
- 指入力は筆圧が取れないため、線幅を速度から推定する。
- サンプリング後、移動平均で平滑化し、Ramer–Douglas–Peucker で間引いて保存する。

### 7.3 出題ロジック

2つの目的の重み付けとして定義する。

- **収集カバレッジ優先**（未収集の文字を優先）… フォント未完成のあいだ重みを大きくする
- **難易度優先**（苦手な文字を反復）… フォント完成後に重みを大きくする

難易度は Sample の Score（お手本との形状距離、画数一致、枠への収まり、所要時間、書き直し回数）から算出。
**母集団は本人の履歴のみ**（全ユーザー統計はサーバーが必要になるため採らない）。

**「はみ出し」ではなく「枠への収まり」を測る。** 書き取り面は入力を枠内に丸めるため
（4.1 の正規化 em 空間）、はみ出しそのものは記録に残らない。代わりに、お手本と比べて
小さすぎないか・寄りすぎていないかを見る。フォントに載せたとき字の大きさが揃わないのは、
はみ出しではなくこちらが原因になる。

**重み付けの実際**:

- 未収集をどれだけ優先するかは、その人の充足率がそのまま決める（`1 - 充足率`）。
  埋まっていないうちは未収集を、埋まるにつれて苦手な字を出す
- 苦手さは**直近5回の試行**だけを見る。昔うまく書けなかったことを引きずらせない
- 書けなかった字（`rejected`）も母集団に入れる。書けなかったという事実そのものが、
  その字が苦手だという証拠になる
- **どの字にも出る目を残す**（重みを 0 にしない）。0 にすると、その字は二度と出ない
- 上位から順ではなく**重みつきの抽選**にする。順に出すと同じ字が続いて飽きる
- ひとまとまりは **5 字ぶん**。1 セッション 3〜5 分（SPEC 7.1）に収まり、終わりが見える。
  語の長さはまちまちなので、語数ではなく字数で区切る

**何を書くかを子供に選ばせない導線（おまかせ）を、画面のいちばん上に置く。** 46 字の
一覧から自分で選べる子ばかりではないし、選ばせると書ける字ばかりを選ぶ。

**出された語はやめられる。** 知らない語・書けない語が出ると、そこで手が止まる。
やめると次の語へ行く。**自分で選んだ語には置かない**（戻ればいいので、同じことを
する道が 2 つになる）。やめたことは声でも言う（黙って別の字に変わると、自分が
何かを間違えたのかと思う子がいる）。

**おまかせは 1 字ずつではなく語で出す。** 書いた字がことばになるほうが手応えがある。
出したい字（未収集・苦手）を多く含む語ほど出やすくする。語の重みは中の字の重みの
**平均**（合計にすると長い語ばかりが出る）。

この結果、**語に出てこない字はおまかせでは永久に出てこない**。そのため:

- 同梱の単語帳は、**その文字種の字に語だけで行き着けるように作る**（テストで縛る）。
  例外は今の日本語の語に出てこない ヲ・ヂ・ヅ・ゥ の 4 字
- どの字が語に出てこないかを、**管理画面で親に列挙する**（7.6）

### 7.4 単語帳

- 形式は **YAML**（業界標準が存在しないため）。親が Excel で作れるよう **CSV インポートも受ける**。
  絵ごと持ち出すときは zip の **`.asodict`**（7.4.1）。
- 単語帳は「練習用の文字列の供給源」であり、意味の学習が目的ではない。
- 絵は大きく出す。**字が読めない子は絵でしか語を選べない。** ただし書き取り画面では
  画面の広さで大きさを変える（書き取り面を押しつぶしては本末転倒）。
- 単語帳は**いくつでも作れる**（「どうぶつ」「うちのことば」など）。名前と語は
  管理画面でその場で直せる。「保存」ボタンは置かない（7.6 の版と同じ）。
- 単語帳には **内蔵** と **自分の** の 2 種類がある（7.4.3）。
- 単語帳**そのものは人ごとに分けない**。分けるのは「誰にどれを出すか」だけで、
  それは `User.wordBooks` が持つ。上の子には漢字入りの語、下の子にはひらがなの語、
  という使い分けができる。**最後の 1 つは外させない**（全部外すと、おまかせも
  ことばも子供の画面から消える）。
- 読みを書かなかったときは、ことばをそのまま読みにする。かなの語ではそれで足りるし、
  そこで止めると入力が進まない。

### 7.4.3 内蔵の辞書

`assets/words/` に置いた `.yaml` / `.asodict` は、**アプリに入っている辞書**として
起動のたびに読み込まれる。親が取り込まなくても最初からあり、要る要らないは
**人ごとのチェック**で切り替える（7.4 の割り振りと同じ仕組み）。

- **一覧は資産そのものから引く。** 並びをコードに持つと、辞書を足したときに
  書き足し忘れて出てこない
- **読めない辞書は、その 1 冊が出ないだけにする。** 手で直した単語帳ファイルを
  置くこともある。それでアプリが開かなくなってはいけない
- **開くたびに合わせる。** 足りないものを入れ、アプリから外れたものを片づける。
  空のときだけ入れる作りにすると、あとから足した辞書が、すでに使っている端末に
  出てこない。**中身は上書きしない**
- **内蔵は直せない・消せない。** 直せると「元に戻す」道が要る。消せても開き直すと
  戻ってくるので、消せたように見えて戻る、といういちばん分かりにくい振る舞いになる。
  直したい人は**コピーを作る**（1 タップ）。要らない人はチェックを外す
- **内蔵であることが分かるようにする。** 一覧に「内蔵」の印を出す。
  直せない・消せないことは、名前だけでは伝わらない

#### 動作確認用の辞書

**ファイル名が `_` で始まる資産は、デバッグでだけ読み込む。**
`.gitignore` で除いてあるので**コミットされず、素の取得から作ったリリースには
存在しない**。手元で試す辞書を、配るものに混ぜないため。

- 一覧では「動作確認用」と、内蔵とは別の色の印を出す
- 配るものの保証（語が集める字を覆っているか等）を確かめるテストは、
  この辞書を見ない。手元に何を置いても、テストの結果は変わらない
- フォルダ（YAML ＋ 絵）から `.asodict` を作るには `tool/pack_word_book.dart`
- CSV の列は **ことば・よみ・タグ** の順。1 行目が見出し（`text` / `ことば` など）なら
  読み飛ばす。タグは列の区切りに使うカンマでは書けないので、空白か「、」で分ける。
- 読めなかったときは、**どこが悪いのかを言う**（何行めの語か）。直せない指摘は、
  直しようがないのと同じ。

```yaml
version: 1
name: どうぶつ
words:
  - text: ねこ
    reading: ねこ
    image: cat.png      # 任意。単語帳ファイル（.asodict）の images/ にある名前
    tags: [どうぶつ]
  - text: "[ウルトラマン]オメガ"   # かっこの中は書かせない
    reading: うるとらまんおめが
```

### 7.4.0 語の一部だけを書かせる

**`[かっこ]` で囲んだところは、出しておくだけで書かせない。**
「[ウルトラマン]オメガ」なら オメガ の 3 字だけを書く。

長い名前をぜんぶ書かせると 1 セッション（3〜5分）で終わらないし、4歳には長すぎて
何を書いているのか分からなくなる。かといって オメガ だけを出したのでは、
子供がその語だと分からない。

- **並びからは外さない。** 出しておく字も画面に出す。何の語を書いているのかは、
  そこが見えていないと分からない。書く枠とは色を変える
- **何も見ずに書くモードでも、出しておく字は伏せない。** 書かせない字を隠しても
  手がかりが減るだけ
- 充足率も出題の重みも、**書かせる字だけ**で決まる
- 書けるかどうかの判定（収集対象の文字種）も**かっこの中は見ない**。
  「[東京]スカイツリー」は漢字を集めていなくても書ける
- **書かせる字が 1 つも無い語は受けない。** 練習に出しようがない
- かっこが閉じていなければ、そこから先ぜんぶを出しておく扱いにする。
  取り込んだ単語帳で 1 か所書き損じただけで、読み込みを断るほどのことではない

### 7.4.1 単語帳の出し入れ

| 形式 | 中身 | 使い道 |
|---|---|---|
| `.yaml` | 文字だけ | テキストエディタで直す・差分を見る |
| `.csv` | 文字だけ（取り込みのみ） | Excel で作る |
| **`.asodict`** | `words.yaml` ＋ `images/` の zip | 絵ごと人に渡す |

拡張子を `.dict` にはしない。辞書ファイルの拡張子として広く使われていて、端末の中で
取り違えられる。控えの `.asoglyph` と揃えて `.asodict` とする。

書き出す形は選ばせる。使い道で変わる（人に渡すなら絵ごと、自分で直すなら YAML）。

### 7.4.2 語に添える絵

**字が読めない子は、絵でしか語を選べない。** 読みを声で聞かせるだけでは、一覧から
選ぶという操作が成り立たない（SPEC 2 の「文字に依存しないUI」）。

- 受けるのは **PNG・JPEG・WebP・SVG**。SVG も受けるのは、線画のほうが小さくて
  幼児にも分かりやすいため。WebP は同じ絵が小さくなるので、容量で切っている
  この設計と相性がよい
- **大きさ（画素数）ではなく容量で切る。1 枚 512 KB まで。** 画素数で切ると、画素を
  持たない SVG を測れない。容量ならどの形式でも同じ物差しで測れる
- **縮小はしない。** 勝手に縮めると、親が選んだ絵と出てくる絵が違うものになる。
  大きすぎる絵は断って、何 KB あったかを見せる
- 絵は書き取り画面にも出す。**何も見ずに書くモードでも出す**（絵は字を教えないので、
  音だけで書くという前提は崩れない）
- どの語からも指されなくなった絵は片づける。絵は記録ではなく持ち物であり、
  4.1 の「削除しない」は書いた記録の話
- **ファイルを落として入れられる。** 親は絵をファイルとして持っている。選ぶ画面を
  開いて探し直すより、そのまま落とせるほうが早い。落とせるのは web と
  デスクトップだけで、iOS・Android では「絵を選ぶ」が唯一の道になる

- 子供が読めない語を出さないよう `reading` を必須とする。
- **読みはひらがなに揃える。** 読みは声で読み上げるためだけにあり、表記ではない。
  揃えないと、同じ「バス」に「ばす」「バス」「BUS」が混ざり、読み上げのエンジンが
  字面をそのまま読むぶんだけ読まれ方も揺れる。
  - カタカナは**ひらがなに直す**（打っている手元で直す。決めたあとに黙って直すと、
    自分が打ったものと違うものが残ったように見える）
  - **長音符「ー」は残す。** ひらがなではないが、落とすと「あーく」が「あく」になり、
    読み上げが別の語になる。空白も残す（「いち に さん」と区切りたいことがある）
  - 漢字・英数字・記号は落とす。読み上げられないか、読み上げても子供に通じない
  - 取り込んだ単語帳も同じ規則で通す。ひらがなが 1 字も残らない読みは、
    そう言って断る
  - 読みを書かなかったときは、**かっこを外してひらがなに直したことば**を読みにする
- 収集対象の文字種に含まれない文字を含む語は、出題候補から自動的に除外する。

### 7.5 ユーザー管理

- 情報は人ごとに完全に分離する。記録も版も `userId` を持ち、ストアはいま選ばれて
  いる人のぶんだけを読む。ほかの人の記録はメモリにも載せない。
  切り替えは 1 か所（`Session`）に閉じ込める。記録・版・選択中の人が
  ばらばらに切り替わる状態を作らないため。
- **1 人目は自動で作る。** 設定を求める画面から始めない。アプリは開いた瞬間に
  書ける状態であってほしい。名前と印はあとから管理画面で直せる。
- **人の切り替えは、2 人以上いるときだけ子供向け画面に出す**（SPEC 9）。
- ユーザー切り替え時のパスコードロックを**可能に**する。**既定は無効**。
- バックアップ／エクスポートを提供する（端末故障で子供の字が消えることは許容できない）。
  書く人・記録（運筆を含む）・版・単語帳（絵を含む）・いま書いている人を
  1 つのファイルにまとめる。
  単語帳を含めるのは、親が作って直すものだから（端末が壊れたら作り直しになる）。
  戻すときは**上書きではなく重ね合わせ**とし、いまある字は消さない。
  ただし、まだ誰も何も書いていない端末に限り、開いたときに自動で作った空の
  1 人を片づける（同じ名前が 2 つ並ぶのを避けるため）。
  出す先は共有シート任せで、こちらからどこかへ送ることはしない（SPEC 3）。

### 7.6 管理画面（保護者向け）

- パスコードでロック**可能**。**既定は無効**。
- パスコードは Keychain / Keystore に保存。忘れた場合の復旧手段を用意する。
- 7.5 のロックとは別のパスコードとする。
- 主要UI:
  - **学年・文字種ごとの充足率リング**（漢字は1年生だけで80字あるため、これが主画面になる）
  - 収集対象の文字種選択
  - 単語帳の作成・編集と、**人ごとの割り振り**（7.4）
  - **ことばに出てこない字の列挙**。おまかせは語で出すため（7.3）、ここに挙がった字は
    その導線では出てこない。親が語を足すか、一覧から直に選ばせることになる
  - **書いたことばの記録の削除**（4.2）。語ごとにも、まとめても消せる
  - FontRecipe の作成・複製・編集・フォント出力
  - 字の一覧と、特定文字の版の差し替え（`charRules`）
  - 時系列スライダー（日付をドラッグするとフォント見本が変化する）。
    「あの頃」がいつなのかは日付を数字で見ても分からないため、字そのものが
    変わるのを見て決められるようにする。見本は数文字だけ出す
    （動かすたびに運筆を読み直すので、数が増えると付いてこない）

版は変えたその場で保存する。「保存」ボタンは置かない。押し忘れで版が消える
導線を作らない。版を消しても Sample は消えない（版は導出ビューでしかない）。

### 7.7 フォント生成と出力

- 形式は **TTF と OTF**。
- 出力のたびに **なぞった字を混ぜるかを選ぶ**（7.1）。既定は混ぜない。
  混ぜる場合はモードを問わず最新の筆跡を採る。
- 生成は端末内で完結。
- 出力先はファイル共有（AirDrop / メール / Drive 等）。
  **iOS はフォントのインストールに構成プロファイルが必要、Android は非 root で system フォントを追加できない**ため、
  端末への直接インストールは提供しない。アプリ内プレビューと共有を出口とする。

## 8. フォント生成パイプライン

```
Sample.strokes (em 0..1000)
  → 線幅の決定（筆圧 or 速度）
  → ラスタ化: PictureRecorder で 1024px にレンダリング
  → 輪郭追跡（marching squares）
  → Ramer–Douglas–Peucker で簡略化
  → ベジェフィット（3次）
  → [OTF] CFF テーブルへ / [TTF] 2次ベジェへ近似して glyf テーブルへ
  → cmap / hmtx / head / hhea / maxp / name / OS/2 / post を構築
```

### 8.1 輪郭化はラスタトレース方式を採る

手書きは「中心線」、フォントは「閉じた輪郭」であるため変換が必要。方式の選定:

| 方式 | 評価 |
|---|---|
| Dart のブーリアン演算ライブラリ（clipper2） | v0.0.3・更新停滞・web で64bit整数制約。依存先として弱い |
| Flutter 標準 `Path.combine(union)` | 堅牢だが `dart:ui` の Path はベジェ命令を読み出せず、結局サンプリング＋再フィットが必要 |
| **ラスタトレース（採用）** | 純Dart・依存ゼロ。自己交差や画の重なりを考えなくてよい。**練習画面で子供が見た線とフォントの字形が完全に一致する** |

### 8.2 ライブラリ

- **OTF (CFF)**: [fontify](https://github.com/westracer/fontify) / fontify_plus / icon_font_generator が
  純Dart・MIT・外部依存なしで SVG パス → OTF を実装済み。流用する。
- **TTF (glyf)**: 上記は**OTF出力のみ**（TTF はロードマップ止まり）。
  3次→2次ベジェ近似と `glyf` / `loca` テーブルのライタを**自作する**。
  **これが最大の技術リスクであり、L1 で最初に潰す。**

### 8.3 フォントメタデータ

- em = 1000、ascender / descender / ベースラインを固定値で定義。
- 字幅: 和文 = 1000、数字・ラテン = 500。
- `name` テーブル: フォント名（ユーザー指定）、製作者 = sharkpp、URL = sharkpp.net。
- `.notdef` と半角スペースは常に含める。
- 未収集文字は cmap に載せない（6.3 の不変条件）。
- ヒンティングは行わない。

## 9. UI・レイアウト方針

- 書き取り面は `AspectRatio` で正方形を保ったまま最大化し、周囲のUIだけ再配置する。
- `LayoutBuilder` ＋ `shortestSide` 600dp をブレークポイントとする。
  - スマホ（< 600dp）… 縦積み
  - タブレット（≥ 600dp）… キャンバスと単語リスト／お手本を横並び
- 低遅延インク: CustomPainter を「確定した画の層（Picture にキャッシュ）」と
  「描画中の1画だけの層」に分け、`RepaintBoundary` で分離する。
- タップターゲットは 64dp 以上。子供向け画面はタップのみで完結させる
  （ドラッグ・スワイプ・長押しは3〜4歳には通じない）。

## 10. 配布

### 10.1 Web 版（PWA）

サーバー側の処理を持たない（SPEC 3）ため、置くだけで動く。ホーム画面に置けて、
オフラインでも開けるようにする。

- **サービスワーカーは自前で持つ。** Flutter のものは 3.44 で非推奨になり、
  中身は「自分を登録解除するだけ」になった。Flutter 側の登録を頼まず
  （`serviceWorkerSettings` を渡さない）、`web/sw.js` を登録する。
  同じ場所には 1 つしか登録できないため、両方は置けない
- **取りに行ってから、駄目ならキャッシュ**（network-first）。Flutter の出力は
  ファイル名に中身の指紋が付かない（`main.dart.js` も `assets/` も名前が変わらない）。
  キャッシュを先に見ると、直したものが古いまま出続け、直す手立てが利用者側
  （サイトのデータを消す）にしかなくなる。network-first なら、こちらが直せば
  次にオンラインで開いたときに必ず治る
- **置き場を消さないよう頼む**（`navigator.storage.persist()`）。空き容量が減ると、
  ブラウザは断りなく IndexedDB を捨てることがある。捨てられると集めた字が
  丸ごと消える。断られても書けなくはならないので、結果で画面は変えない
- **画面の向きは縛らない**（`orientation: any`）。タブレットは横で使う（SPEC 9）
- **iOS では、ホーム画面に置いたアプリと Safari で置き場が分かれる。**
  Safari で書いた字は、ホーム画面から開いたアプリには出てこない。
  先に控えを書き出してから置き換えることになる（SPEC 7.5）
- アイコンは `tool/generate_web_icons.dart` で作る。字は描かない。フォントを
  焼き込むと持たない環境で豆腐になり、KanjiVG の字形を使うとアイコンが
  その二次的著作物になる（SPEC 6.3）

## 11. 法務・ストア

- 子供の筆跡は個人情報。**端末内で完結させることで対応を簡素化する。**
- 子供向けカテゴリ（Kids Category / Designed for Families）は広告・外部リンク・データ収集に
  厳しい制約がある。収益化方針は未定だが、**広告は入れない**前提で設計する。
- KanjiVG のクレジットとライセンス表記をアプリ内に置く（6.3）。

## 12. 実装順序

動くものを積み上げる。各段階は、それ自体で完結した製品であること。

| | 内容 | 完了条件 |
|---|---|---|
| **L1** | ひらがな清音46字。収集 → 輪郭化 → フォント出力を貫通 | 1グリフの **TTF と OTF が両方出る**（最大の技術リスクを最初に潰す） |
| **L2** | 濁音・半濁音、小書き、数字、カタカナ。KanjiVG による書き順再生（番号・矢印）。マス目・3モード | かな171字のフォントが出る |
| **L3** | ユーザー管理、管理画面、パスコード、FontRecipe（文字種選択・時点指定・字の差し替え） | 「あの頃の文字」が作れる |
| **L4** | 単語帳（YAML / CSV）、出題ロジック、採点と難易度統計 | 単語で練習でき、苦手な字が出やすくなる |
| **L5** | 漢字（学年別）、ラテン文字（4本線ガイド） | 学年を選んでフォントが作れる |

## 13. やらないこと

明示的に対象外とする。

- クラウド同期・サーバー側でのフォント生成
- 全ユーザー横断の難易度統計
- 端末へのフォント直接インストール
- プロポーショナルなラテン字幅、ヒンティング
- 履歴の自動刈り込み（4.5）
- 清音＋濁点からの濁音合成（5.1）
- 後方互換のための移行レイヤ（AGENTS.md の方針に従う）

## 参考

- [KanjiVG](https://kanjivg.tagaini.net/) — CC BY-SA 3.0 / Ulrich Apel
- [学年別漢字配当表](https://ja.wikipedia.org/wiki/学年別漢字配当表) — 2020年度〜 1026字
- [fontify](https://github.com/westracer/fontify) — MIT / 純Dart / OTF 出力
- [clipper2 (Dart)](https://pub.dev/packages/clipper2) — 採用せず（8.1）
- 最判平成12年9月7日 民集54巻7号2481頁（ゴナU書体事件） — 書体の著作物性（6.3）
